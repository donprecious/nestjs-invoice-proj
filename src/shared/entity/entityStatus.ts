export const entityStatus = {
  pending: 'pending',
  success: 'success',
};

export const invitationStatus = {
  pending: 'pending',
  canceled: 'canceled',
  accepted: 'accepted',
};

export enum RoleAccessableType {
  private = 'private',

  public = 'public',
}
